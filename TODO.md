# TODO List for Removing floor_number Duplication

- [x] Remove the dynamic addition of `floor_number` field from `ResidentProfile` in `add_fields.py`
- [x] Verify that `floor_number` is accessible via the `Unit` relation in `ResidentProfile`
